@page
@model Web.Pages.DistanceConverterModel
@using ConsoleAppProject.App01
@{
}

<header class="jumbotron">
    <h1>Distance Converter</h1>
    <h2>by Derek Peacock</h2>
</header>

<section>

    <form class="form-group border-dark" method="post">

        <h4>Select Your from distance unit</h4>
        <br/>
        <p>
            <select asp-for="App01.fromUnit">
                <option disabled selected>Choose</option>
                <option>@DistanceConverter.FEET</option>
                <option>@DistanceConverter.METRES</option>
                <option>@DistanceConverter.MILES</option>
            </select>
        </p>

        <h4>Enter your from distance</h4>
        <br/>
        <p>
            <input asp-for="App01.fromDistance" type="text"/>
        </p>

        <h4>Select your to distance unit</h4>
        <br/>
        <p>
            <select asp-for="App01.toUnit" 
                asp-items="@Html.GetEnumSelectList<DistanceUnits>()">
            </select>

            <button class="btn btn-success" type="submit">Calculate</button>
        </p>

        <p class="lead  alert alert-primary">
            @ViewData["Message"]
        </p>

    </form>

</section>